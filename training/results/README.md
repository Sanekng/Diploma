# Training results

Training outputs, logs, and generated plots are local artifacts and are intentionally excluded from Git. Keep important experiment results in a release artifact or external object storage.
