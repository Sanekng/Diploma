# Prepared training data

This directory contains generated/preprocessed data and is intentionally excluded from Git. Recreate it with the training preparation workflow after restoring the source dataset.
