# IAS model artifacts

The trained Keras model files in this directory are intentionally excluded from Git because they exceed GitHub's regular file-size limit. Restore `fire_detection_model.keras` here from the project's external model artifact storage before starting the Image Analysis Service.